// It almost certainly seems like overkill to have this in a separate file
// but I like the break out of this as in a larger app there could be a 
// good deal of set up to do before calling start on the router.

$(function(){ 
	KivaLoansApp.start() 
});